cmd_ws_auto_back_and_forth: fix negation In the Conversion to `parse_boolean` for `cmd_ws_auto_back_and_forth` (#3098), the `!` was never removed (was previously using `strcasecmp`) causing the setting to be the opposite of what it should be.
